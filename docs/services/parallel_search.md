# Parallel Search

An MCP service that connects your workspace to Parallel Search's endpoint, letting assistants run parallel web searches through the Parallel Search API.

## Configuration

Set the **API key** config field to your Parallel Search API key.