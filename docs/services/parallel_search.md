# Parallel Search

An MCP service that connects your workspace to Parallel Search's public endpoint, letting assistants run parallel web searches through the Parallel Search API.

## Configuration

No configuration is required — the endpoint is public and needs no API key or headers. Just create the service.