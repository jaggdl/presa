<div class="mx-auto md:w-3/4 w-full">
  <h1 class="font-bold text-4xl">Services</h1>

  <div class="grid grid-cols-2 sm:grid-cols-2 lg:grid-cols-3 gap-4 mt-8">
    <% @kinds.each do |kind| %>
      <%= link_to new_kind_service_services_path(kind), class: "flex items-center gap-3 p-4 rounded-lg border border-zinc-700 bg-zinc-900 hover:bg-zinc-800 transition" do %>
        <%= service_icon(Service.class_for_kind(kind).new, size: "h-9 w-9") %>
        <span class="font-medium text-zinc-100 mr-auto"><%= kind.humanize %></span>
        <%= icon "plus", class: "size-6 text-zinc-500" %>
      <% end %>
    <% end %>
  </div>

  <% if @services.any? %>
    <h2 class="font-bold text-2xl mt-10">Your services</h2>
    <table class="w-full text-left mt-4">
      <thead>
        <tr class="border-b border-zinc-800 text-sm text-zinc-500">
          <th class="py-2">Name</th>
          <th class="py-2">Kind</th>
          <th class="py-2">Workspaces</th>
          <th class="py-2"></th>
        </tr>
      </thead>
      <tbody>
        <% @services.each do |service| %>
          <tr class="border-b border-zinc-800">
            <td class="py-2"><%= link_to service.name, service_path(service), class: "text-blue-500 hover:underline" %></td>
            <td class="py-2">
            <%= service_icon(service, size: "h-9 w-9") %>
            <span class="ml-3"><%= service.kind %></span>
          </td>
            <td class="py-2"><%= service.workspaces.map(&:name).presence&.join(", ") || "–" %></td>
            <td class="py-2 text-right">
              <%= link_to "Edit", edit_service_path(service), class: "text-zinc-400 underline hover:no-underline mr-3" %>
              <%= button_to "Delete", service_path(service), method: :delete, class: "text-red-400 underline hover:no-underline", form: { data: { turbo_confirm: "Delete this service?" } } %>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
  <% else %>
    <p class="text-zinc-500 mt-6">No services yet.</p>
  <% end %>
</div>