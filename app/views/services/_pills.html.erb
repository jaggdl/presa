<span class="mt-1 flex flex-wrap items-center gap-1">
  <span class="pill -ml-1"><%= entity.category.to_s.tr("_", "-").humanize %></span>
  <% entity.tags.each do |tag| %>
    <span class="pill pill--subtle"><%= tag %></span>
  <% end %>
</span>