<div>
  <p class="text-xs text-zinc-400 mb-2">Add the server via CLI:</p>
  <%= render "shared/copy_block", content: "claude mcp add --transport http #{client_name} #{base_url}/mcp/sse \\
  --header \"Authorization: Bearer #{raw_token}\"" %>
</div>