<p class="text-xs text-zinc-400 mb-2">Add the server via CLI:</p>
<pre class="text-xs text-zinc-200 overflow-x-auto select-all whitespace-pre-wrap break-all">claude mcp add --transport http <%= client_name %> <%= base_url %>/mcp/sse \
  --header "Authorization: Bearer <%= raw_token %>"</pre>