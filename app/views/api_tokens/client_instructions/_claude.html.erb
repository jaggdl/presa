<p class="text-xs text-zinc-400 mb-2">Add to <code class="text-zinc-300">claude_desktop_config.json</code>:</p>
<pre class="text-xs text-zinc-200 overflow-x-auto select-all whitespace-pre-wrap break-all"><%= JSON.pretty_generate({ "mcpServers" => { client_name => { command: "npx", args: ["-y", "supergateway", "--sse", "#{base_url}/mcp/sse", "--header", "Authorization:Bearer #{raw_token}"] } } }) %></pre>